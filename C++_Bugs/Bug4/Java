class F {
    // Destructor is not explicitly defined in Java, as garbage collection handles it.
}

class G {
    // Destructor is not explicitly defined in Java, as garbage collection handles it.
}

// Java does not have a direct equivalent for C++'s attribute for initialization priority.
// You would typically initialize objects in a static block or constructor.
G _S_head_owner = new G();

