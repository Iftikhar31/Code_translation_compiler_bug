using System;

class Program
{
    static void Main()
    {
        int a = 0, b = 0;
        int c = 0;
        if (a + 2 != 0)
            c = 1;
        int d = (1 + c - 2 + (c == 1 ? 1 : 0)) - 1;
        b = ((d + 1) << d) + d;
        Console.WriteLine("{0:X}", b);
    }
}
